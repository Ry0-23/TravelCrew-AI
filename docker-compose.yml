version: "3.9"

services:
  travelcrew:
    build: .
    container_name: travelcrew-ai
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      # Persist ChromaDB vector store across restarts
      - chroma_data:/app/chroma_db
    restart: unless-stopped

volumes:
  chroma_data: